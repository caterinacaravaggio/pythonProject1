---
appName: userService

routes:
  insert:
    url: '/userService/insertUser'
  get:
    url: '/userService/getUser'

database:
  mongodb:
    host: 'localhost'
    port: '27017'
    db: users


logger:
  filename: 'tmp/info.log'
  level: INFO
  propogate: no